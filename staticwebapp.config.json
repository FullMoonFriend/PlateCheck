{
    "navigationFallback": {
        "rewrite": "/index.html",
        "exclude": ["/api/*", "/_framework/*", "/css/*", "/js/*", "*.{png,jpg,gif,ico,svg,woff,woff2}"]
    },
    "routes": [
        {
            "route": "/api/*",
            "allowedRoles": ["anonymous"]
        }
    ]
}
